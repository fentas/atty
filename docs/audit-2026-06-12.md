# Full-repo audit — 2026-06-12

Source: in-depth review of the whole repo (Zig proxy core, LLM module, security_guard,
atty-guard Rust sidecar + eBPF, config/build/CI/tests). All suites were green at audit time
(unit/itest/e2e 32/32, `cargo test` 244/244). Each finding below has a tracking issue and a
TODO list; work proceeds one issue at a time on its own branch → `/review-loop` → merge.

Labels: every issue carries `audit-tracked` so future reviews skip them.

## Critical

- [x] **#413** — LLM dialog-mode exec bypasses control-byte sanitization (keystroke injection) ✅ PR #437
  - [x] Newline-preserving control-byte strip on the fence body before `wrapForBracketedPaste`
  - [x] Apply in both JSON + fenced exec arms
  - [x] Overflow fallback aborts instead of raw paste (`dialog.zig:381-385`)
  - [x] Regression test: `\r`, ESC, literal `\x1B[201~` neutralized
- [x] **#414** — history: `deleteHistoryMatch` truncates large files; `loadRecent` reads head not tail ✅ PR #438
  - [x] `loadRecent` seeks to tail
  - [x] `deleteHistoryMatch` stream-filters the real file
  - [x] Preserve original raw lines / zsh timestamps
  - [x] Tests: >1 MiB keeps newest; delete preserves non-matching + timestamps (boundary-peek test deferred → #439)
- [x] **#415** — atty-guard: warn-event subscription has no auth + leaks slots (host-wide DoS) ✅ PR #440
  - [x] Auth: root-gate `parent_pid_tree==0`; ownership-check non-zero (+ starttime sandwich)
  - [x] Separate subscriber cap (total 16 / per-UID 4), exempt from classify cap
  - [x] Detect peer disconnect with zero traffic (recv MSG_PEEK; EINTR-safe; squat-byte close)
  - [x] Test: deregister-by-token; cross-UID rejected; peer_disconnected matrix

## High

- [x] **#416** — atty-guard: V2-J auto-Block fires on correlated signals (blocks canonical installs) ✅ PR #441
  - [x] Dedupe hits by overlapping span (distinct-signal count; Tier-2 counts separately)
  - [x] Fix misleading test; assert fsSL install stays Warn (+ multi-atom-still-blocks + attribution tests)
- [x] **#417** — atty-guard: eBPF enforcement never engages on install (silent fail-open) ✅ PR #442
  - [x] install.sh builds+copies `.bpf.o` to `/usr/lib/atty-guard` (hard-fail on missing BTF/clang/bpftool/make)
  - [x] Add `CAP_MAC_ADMIN` to the drop-in
  - [x] Corrected the doc to the one-level LSM limitation (deepening = future work)
- [x] **#418** — security_guard: sticky silent fail-open + `trust_seeded` set on failure ✅ PR #443
  - [x] Re-probe daemon every 20 Enters when disabled
  - [x] One-line stderr notice on downgrade; `daemon_trust_seeded` only on seed success (recovery test → #444)
- [x] **#419** — proxy: stdin split-sequence corruption (line_state) + whole-read keymap match ✅ PR #446
  - [x] `applyInput` carries a partial CSI across reads (no continuation-byte injection)
  - [ ] Prefix-match single-keystroke bindings → follow-up #445 (higher-risk, split out)
  - [x] Recognize `1;<mod><final>` cursor-motion (final-byte keyed); regression tests
- [ ] **#420** — llm: pick-list u16 underflow + musl `Stat` in recall + worker stack overflow
  - [ ] Clamp `cc` in overlay pick-list
  - [ ] Replace hand-rolled `Stat` with `std.Io.File.stat`
  - [ ] Larger worker stack or heap decode scratch
- [ ] **#421** — atty-guard: reachable UTF-8 panics from client input (npm_parser, osv)
  - [ ] Byte-compare in `npm_parser.rs:84`; char-boundary truncate in `osv.rs:375`
  - [ ] Unicode tests
- [ ] **#422** — atty-guard: atom pipeline hardening (unbounded accumulation, min-len flood, write perms)
  - [ ] Cap accumulation inside the walk; raise quality gate
  - [ ] `create_new`+PID tmp+0640 like `write_snapshot`; reject `#`-leading atoms
  - [ ] Tests

## Medium

- [ ] **#423** — atty-guard: `trust_list` overflows 16 KiB client buffer; reason truncation unenforced
- [ ] **#424** — atty-guard: OSV unreachable via 50 ms timeout + npm amplifier + classify pid gate bypass
- [ ] **#425** — atty-guard: Zig client first-substring parse + no response `id` validation
- [ ] **#426** — config: `ghost.list_count` defaults to 2 but docs say 0
- [ ] **#427** — config: resolver ignores unknown decls; daemon config lacks `deny_unknown_fields`
- [ ] **#428** — tests: `make integration-test-full` fails (deleted `atom_fetcher_lolbas` scenario)
- [ ] **#429** — docs: `config.def.zig` Terminal section is factually wrong
- [ ] **#430** — llm: worker subprocess stdin write deadlocks on streaming CLIs
- [ ] **#431** — ci: musl release targets never compiled before tag push
- [ ] **#432** — atty-guard: `write_atomic` never fsyncs (crash drops operator detections)
- [ ] **#433** — tests: daemon-Block path, root-early-return auth tests, atom-overlay all uncovered

## Low

- [ ] **#434** — docs: CLAUDE.md + module/atuin/llm doc drift
- [ ] **#435** — hygiene: root PNGs, `__pycache__`, `build.zig.zon` paths, Darwin portability

---

## Working order

Critical → High → Medium → Low, top to bottom. Each: branch `fix/<scope>-<issue>` →
implement + tests → `zig fmt` / `cargo fmt` → `/review-loop` → squash-merge → close issue →
tick boxes here.
