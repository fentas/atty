/target
/Cargo.lock
# Generated by `make` in atty-guard/ebpf — vmlinux.h is large
# (~3 MB) and BTF-specific; rebuilt locally per kernel. The .o is
# the daemon's runtime artifact (loaded via libbpf-rs).
ebpf/vmlinux.h
ebpf/*.o
